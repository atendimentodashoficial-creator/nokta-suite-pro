// DISABLED: This scheduler is replaced by external cron (cron-job.org)
// The external cron calls the edge function directly, avoiding duplicate sends
// To re-enable, uncomment the code below

export function useAvisosAgendamentoScheduler() {
  // Intentionally empty - cron is handled externally
}
